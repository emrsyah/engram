{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot --port=3030 src/index.ts",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "@alphonse/api": "workspace:*",
    "@alphonse/auth": "workspace:*",
    "@alphonse/db": "workspace:*",
    "@alphonse/env": "workspace:*",
    "@hono/trpc-server": "^0.4.0",
    "@trpc/server": "catalog:",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@alphonse/config": "workspace:*",
    "@types/bun": "catalog:",
    "tsdown": "^0.21.9",
    "typescript": "catalog:"
  },
  "trustedDependencies": [
    "supabase"
  ]
}
